{
  "name": "gilwell-media",
  "version": "1.0.0",
  "private": true,
  "description": "Gilwell Media static site (Cloudflare Pages + Workers). Tests only — no build step.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
